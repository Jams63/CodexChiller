import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Job descriptions can be long; allow bigger server action payloads if used later.
  experimental: {},
};

export default nextConfig;
